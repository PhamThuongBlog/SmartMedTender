spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/medbid_dev
  jpa:
    show-sql: true
  flyway:
    clean-disabled: false

app:
  cors:
    allow-all-origins: true

logging:
  level:
    com.medbid: TRACE
    org.hibernate.SQL: DEBUG
